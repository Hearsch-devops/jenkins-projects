apiVersion: v1
kind: Service

metadata:
  name: simple-html-service
  namespace: blue-green-deploy

spec:
  type: NodePort

  selector:
    app: simple-html-app
    version: green

  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080