apiVersion: v1
kind: Service

metadata:
  name: deployment-dashboard-backend
  namespace: progressive-delivery

spec:
  selector:
    app: deployment-dashboard-backend

  ports:

  - port: 5000
    targetPort: 5000

  type: ClusterIP