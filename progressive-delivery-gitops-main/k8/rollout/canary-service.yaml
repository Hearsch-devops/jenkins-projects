apiVersion: v1
kind: Service

metadata:
  name: simple-html-canary-service
  namespace: progressive-delivery

spec:
  type: ClusterIP

  selector:
    app: simple-html-app

  ports:
    - port: 80
      targetPort: 80