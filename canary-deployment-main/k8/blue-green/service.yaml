apiVersion: v1
kind: Service

metadata:
  name: simple-html-blue-green-service
  namespace: blue-green-deploy

spec:
  type: NodePort

  selector:
    app: simple-html-app
    version: blue

  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080